{{debug "⚙️ Rendering nginx config" entity.id}}
server {
    listen 80;
    server_name _;

    location / {
        root /var/www/html;
        try_files $uri $uri/ =404;
        {{ document.meta.content }}
    }
}
